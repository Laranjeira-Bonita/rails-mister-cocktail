<h1>Cocktails#show</h1>
<p>Find me in app/views/cocktails/show.html.erb</p>
<p><%= @cocktail.name %></p>
<% @cocktail.doses.each do |d| %>
  <p><%= d.ingredient.name %> ---------------- <%= d.description %><%= link_to 'remove', dose_path(d), method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% end %>


<%= link_to 'Back', cocktails_path %>
<%= link_to 'new ingredinte', new_cocktail_dose_path(@cocktail) %>
